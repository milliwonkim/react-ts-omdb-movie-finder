import React from 'react';
import { StyledLink2, AppHeader } from '../styles/styles';

const Navbar = () => {
    return (
        <>
            <AppHeader>
                <StyledLink2 to='/'>MOVIE FINDER</StyledLink2>
            </AppHeader>
        </>
    );
};

export default Navbar;
